// Uniform numbers are identity metadata, never a source of simulation randomness.
export function ensureJerseys(s){const used=new Set();for(const w of s.wizards.filter(w=>w.status==='Active')){if(Number.isInteger(w.jersey)&&w.jersey>=0&&w.jersey<=99&&!used.has(w.jersey))used.add(w.jersey);else delete w.jersey;}for(const w of s.wizards.filter(w=>w.status==='Active'&&w.jersey===undefined)){let number=1;while(used.has(number)&&number<100)number++;if(number===100)number=0;w.jersey=number;used.add(number);}}
export function assignJersey(s,w,number){if(!w||w.status!=='Active')throw Error('Only active wizards can change their jersey number.');if(!Number.isInteger(number)||number<0||number>99)throw Error('Choose a whole jersey number from 0 to 99.');const other=s.wizards.find(x=>x!==w&&x.status==='Active'&&x.jersey===number);if(other)throw Error(`#${number} belongs to ${other.name}. Choose another number.`);w.jersey=number;}
